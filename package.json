{
    "name": "technical-assessment",
    "version": "1.0.0",
    "description": "Technical Assessment - A trust-minimised implementation of a multiplayer online game",
    "type": "module",
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "postinstall": "npm install --prefix server --include=dev && npm install --prefix client",
        "node": "cd contracts && npx hardhat node",
        "deploy": "cd contracts && npx hardhat deploy --network localhost",
        "client": "cd client && npx vite",
        "server": "node start-server.js",
        "install-server": "npm install --prefix server --include=dev"
    },
    "devDependencies": {
        "nodemon": "^3.1.14"
    },
    "dependencies": {
        "@geckos.io/phaser-on-nodejs": "^1.3.1",
        "@geckos.io/server": "^3.1.0",
        "phaser": "^4.2.1"
    }
}
